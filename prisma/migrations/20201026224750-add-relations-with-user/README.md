# Migration `20201026224750-add-relations-with-user`

This migration has been generated by Rinat at 10/27/2020, 1:47:50 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "public"."bids" ADD COLUMN "creatorId" text   NOT NULL 

ALTER TABLE "public"."deals" ADD COLUMN "matcherId" text   NOT NULL 

ALTER TABLE "public"."bids" ADD FOREIGN KEY ("creatorId")REFERENCES "public"."users"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."deals" ADD FOREIGN KEY ("matcherId")REFERENCES "public"."users"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20201026212533-postgres-init..20201026224750-add-relations-with-user
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource db {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 enum Role {
   BUYER
@@ -71,9 +71,9 @@
   @@map("users")
 }
 model CompanyModel {
-    id       String  @id @default(cuid())
+  id       String  @id @default(cuid())
   name String @unique
   users UserModel[]
@@ -83,19 +83,21 @@
 }
 model BidModel {
   id       String  @id @default(cuid())
+
+
+  volume Float
+  price Float
+  creatorId String
   type BidType
   resourse Resourse
   coalMark CoalMark
   buyerDeal DealModel  @relation("buyer_bid_deals")
   sellerDeal DealModel @relation("seller_bid_deals")
+  creator UserModel @relation(fields: [creatorId], references: [id])
-  volume Float
-  price Float
-
-
   createdAt DateTime @default(now())
   @@map("bids")
@@ -105,20 +107,22 @@
   id       String  @id @default(cuid())
   buyerBidId String
   sellerBidId String
+  matcherId String
   buyerBid BidModel @relation("buyer_bid_deals", fields: [buyerBidId], references: [id])
   sellerBid BidModel @relation("seller_bid_deals", fields: [sellerBidId], references: [id])
+  matcher UserModel @relation(fields: [matcherId], references: [id])
   createdAt DateTime @default(now())
   @@unique([buyerBidId, sellerBidId])
   @@map("deals")
 }
 model LocationModel {
-    id       String  @id @default(cuid())
+  id       String  @id @default(cuid())
   name String
   code String @unique
```


